import {
  ScreenStateContent,
  ScreenStateDescription,
  ScreenStateIconContainer,
  ScreenStateRoot,
  ScreenStateTitle,
} from './components'

export namespace ScreenState {
  export const Root = ScreenStateRoot
  export const IconContainer = ScreenStateIconContainer
  export const Content = ScreenStateContent
  export const Title = ScreenStateTitle
  export const Description = ScreenStateDescription
}
