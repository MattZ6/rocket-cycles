import { styled } from '@styles/stitches.config'

export namespace ScreenStateContentStyles {
  export const Container = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: '$small',
  })
}
