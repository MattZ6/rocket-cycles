import { styled } from '@styles/stitches.config'

export namespace ScreenStateDescriptionStyles {
  export const Description = styled('p', {
    fontSize: '$default',
    lineHeight: '$default',
    fontWeight: '$regular',
    color: '$defaultLowContrastText',
    textAlign: 'center',
  })
}
