import { styled } from '@styles/stitches.config'

export namespace ScreenStateTitleStyles {
  export const Title = styled('h4', {
    fontSize: '$default',
    lineHeight: '$default',
    fontWeight: '$medium',
    textAlign: 'center',
  })
}
