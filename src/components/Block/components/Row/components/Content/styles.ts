import { styled } from '@styles/stitches.config'

export namespace ContentStyles {
  export const Content = styled('div', {
    flex: 1,
    display: 'flex',
    flexDirection: 'column',
    gap: '$smaller',
  })
}
