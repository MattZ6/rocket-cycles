import { styled } from '@styles/stitches.config'

export namespace RowTitleStyles {
  export const Title = styled('h3', {
    fontSize: '$default',
    lineHeight: '$default',
    fontWeight: '$medium',
  })
}
