import { styled } from '@styles/stitches.config'

export namespace RowStyles {
  export const Container = styled('article', {
    display: 'flex',
    alignItems: 'center',
    gap: '$medium',
  })
}
