import { styled } from '@styles/stitches.config'

export namespace FieldTitleStyles {
  export const Title = styled('h3', {
    fontSize: '$default',
    lineHeight: '$default',
    fontWeight: '$medium',
  })
}
