import { styled } from '@styles/stitches.config'

export namespace FieldHeaderStyles {
  export const Header = styled('header', {
    display: 'flex',
    flexDirection: 'column',
    gap: '$smaller',
  })
}
