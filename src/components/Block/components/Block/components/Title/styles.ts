import { styled } from '@styles/stitches.config'

export namespace BlockTitleStyles {
  export const Title = styled('h2', {
    fontSize: '$medium',
    lineHeight: '$medium',
    fontWeight: '$medium',
  })
}
