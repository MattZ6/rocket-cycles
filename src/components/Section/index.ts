import {
  SectionField,
  SectionRoot,
  SectionRow,
  SectionSeparator,
} from './components'

export namespace Section {
  export const Root = SectionRoot
  export const Field = SectionField
  export const Row = SectionRow
  export const Separator = SectionSeparator
}
