import { Viewport } from '@radix-ui/react-select'

import { styled } from '@styles/stitches.config'

export namespace SelectViewportStyles {
  export const Container = styled(Viewport, {
    padding: '$small',
  })
}
